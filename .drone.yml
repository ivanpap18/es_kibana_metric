---
kind: pipeline
type: docker

steps:
  - name: test
    image: bash
    environment:
    USERNAME:
      from_secret: username
    commands:
      - echo $USERNAME
    when:
      branch: 
        - main     
---
kind: secret
name: password
get:
  path: secrets/data/docker
  name: username
  
trigger:
  branch:
    - main 
  event:
    - push
    - tag   
